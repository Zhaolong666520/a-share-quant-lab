# A-Share Quant Lab 协作规则

本项目用于金融学习与研究，不连接券商，不自动交易，不构成投资建议。

## 所有 AI 必须遵守

1. 原始数据只能新增快照，不能静默覆盖或手工修改。
2. 每份数据必须带有来源、采集时间、复权方式和成交量单位。
3. 策略信号必须至少延迟到下一交易日执行，禁止未来函数。
4. 回测必须显示交易成本、最大回撤、交易次数和基准收益。
5. 不得根据样本外测试结果反复调参。
6. 不得把合成演示数据描述为真实行情。
7. 数据源失败时应明确报告，不得编造或补齐真实行情。
8. 任何结果进入报告前，必须通过测试与数据校验。
9. 查看样本外结果后，不得修改同一实验的切分日期或参数；新想法必须建立新的实验编号。
10. 亏损或落后基准的实验必须保留，不得只展示成功结果。
11. 已经查看过的历史区间只能称为回顾性稳定性诊断，不得重新包装成“全新样本外证据”。
12. 费率、税率和滑点若未引用明确生效规则，只能标记为假设情景，不得描述为当前真实标准。
13. 参数敏感性网格必须在查看结果前固定并保留全部组合，不得只展示表现最好的一组。
14. 日线停牌或涨跌停代理必须明确标注为事后假设，不得冒充交易所当前规则或开盘时可知信息；
    受阻订单要保留原仓位，只在模拟判定成交时计入换手与成本。
15. 正式实验必须记录整理数据文件的SHA-256、数据集ID、日期区间、来源和陈旧状态；数据源失败
    不得被“已有旧数据”掩盖。
16. 账户回测只适用于配置中明确标为可交易的ETF或股票；必须逐日对账现金、整数手数、费用和
    账户权益，任何负现金、非整手或对账差异都必须中止报告。

## 多 AI 分工

- 研究员：只在 `research/` 和 `experiments/` 中提交假设与资料来源。
- 执行者：维护 `src/`、`tests/` 和运行脚本。
- 审核者：检查数据泄漏、复权、成本、停牌和涨跌停处理。
- 人类：批准数据来源与任何真实资金相关决定。

多个 AI 同时工作时应使用独立 Git 分支或 worktree，避免同时修改同一文件。

## 实验记录

每个正式实验在 `experiments/` 中保留一份编号文档，至少记录：问题、固定参数、数据区间、
切分日期、成本假设、结果和下一步。任何AI继续研究前，必须先阅读已有实验记录。

## 数据契约

日线数据的规范字段为：

`symbol, trade_date, open, high, low, close, volume, volume_unit, amount, adjustment, source, ingested_at`

主键语义为 `symbol + trade_date`。本项目第一阶段使用不复权数据，并且不比较不同来源的成交量，因为其单位可能不同。

正式研究先运行 `finance-lab manifest`，并把数据集ID写入实验记录。账户成交使用不复权原始价格；
现金分红只能依据登记日、支付日、每份金额和来源均完整的不可变本地快照入账；快照缺失的区间
不能把账户收益称为完整总回报。拆分合并等其他公司行动仍未建模。
